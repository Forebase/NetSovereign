# Contributing
Use Python 3.12+, `uv sync --all-extras`, and run `pytest`, `ruff check .`, `ruff format --check .`, and strict `mypy`. Keep world meaning provider-neutral; propose architectural changes as ADRs. Do not add operational providers during v0.1.
